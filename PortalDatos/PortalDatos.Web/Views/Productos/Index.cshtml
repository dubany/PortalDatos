@model List<PortalDatos.Web.Models.ProductoModel>
@{
    ViewBag.Title = "Index";
}

<h2>Nuestros Productos</h2>

@foreach (var producto in Model)
{
    <p> @producto.Id @producto.Descripcion</p>
}